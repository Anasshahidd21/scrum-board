export enum ErrorCodes {
  TASK_NOT_FOUND = "Task Not Found!",
  TASK_ADDED = "Task successfully added",
  TASK_ERROR = "Error occured while adding task",
  TASK_DELETED = "Task successfully deleted",
}

export enum StatusCode {
  OK = 200,
  ERR_500 = 500,
  ERR_404 = 404,
}
